# ============================================
# Deploy Finance Tracker Website to VPS
# Builds static site on GitHub runner, then
# deploys to /var/www/financetracker-web via SCP + SSH
# ============================================

name: Deploy Finance Tracker Website

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: '20'
  DEPLOY_DIR: /var/www/financetracker-web

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Upload build to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "dist/"
          target: "/tmp/deploy-financetracker-web"
          rm: true

      - name: Deploy to web root
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            sudo rsync -a --delete /tmp/deploy-financetracker-web/dist/ ${{ env.DEPLOY_DIR }}/
            sudo chown -R nginx:nginx ${{ env.DEPLOY_DIR }}
            rm -rf /tmp/deploy-financetracker-web
            echo "Finance Tracker website deployed successfully!"

      - name: Health check
        run: |
          sleep 5
          STATUS=$(curl -sf -o /dev/null -w "%{http_code}" https://financetracker.web.mohitpalan.me/health || echo "000")
          if [ "$STATUS" = "200" ]; then
            echo "Health check passed!"
          else
            echo "Health check returned $STATUS (site may need DNS/Cloudflare setup)"
          fi
